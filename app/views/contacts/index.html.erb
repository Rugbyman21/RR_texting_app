<h1>Contact Page</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, contact_path(contact) %></li>
  <% end %>
</ul>

<%= link_to "New Contact", new_contact_path %>
<%= link_to "Back to Message", messages_path %>
